# Nested repo — tracked separately
agentic-template/

# Build artifacts
target/
**/target/
node_modules/
**/node_modules/
*.crate

# Editor / OS
.DS_Store

# Live-generated TS fixture from walkthrough_ts test (regenerated each run, not committed)
signed-intents/tests/fixtures/walkthrough_live.json
